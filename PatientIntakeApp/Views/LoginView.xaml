<UserControl x:Class="PatientIntakeApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
    </UserControl.InputBindings>
    <Grid>
        <materialDesign:Card Width="520" UniformCornerRadius="10" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="28">
            <StackPanel>
                <TextBlock Text="Login" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,8" />
                <TextBlock Text="Enter your credentials to access the staging queue."
                           Style="{StaticResource MaterialDesignBody2TextBlock}"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                           Margin="0,0,0,20"/>

                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Username"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,0,12"/>

                <PasswordBox x:Name="PasswordBox"
                             PasswordChanged="PasswordBox_OnPasswordChanged"
                             materialDesign:HintAssist.Hint="Password"
                             Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                             Margin="0,0,0,12"/>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="#D32F2F"
                           Style="{StaticResource MaterialDesignCaptionTextBlock}"
                           Margin="2,0,0,12"/>

                <Button Content="LOGIN"
                        Command="{Binding LoginCommand}"
                        Style="{StaticResource NoRippleRaisedButton}"
                        Height="40"
                        Margin="0,0,0,20"/>

                <materialDesign:Card Background="#0F000000" Padding="12" UniformCornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Test Credentials" FontWeight="Bold" Margin="0,0,0,6"/>
                        <TextBlock Text="User 1: user1 / password" />
                        <TextBlock Text="User 2: user2 / password" />
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>


