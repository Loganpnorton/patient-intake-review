<UserControl x:Class="PatientIntakeApp.Views.ProcessingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <materialDesign:Card Width="500" UniformCornerRadius="8" VerticalAlignment="Center" Padding="24">
            <StackPanel>
                <TextBlock Text="Processing Files" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,24"/>
                
                <TextBlock Text="{Binding StatusText}" Style="{StaticResource MaterialDesignSubtitle2TextBlock}" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,0,16"/>
                
                <!-- Standard slim progress bar (no size/opacity changes) -->
                <ProgressBar Value="{Binding ProgressValue}"
                             Maximum="100"
                             Height="6"
                             Margin="0,0,0,24"
                             Background="{StaticResource PrimaryHueLightBrush}"
                             Foreground="{StaticResource PrimaryHueMidBrush}"
                             BorderThickness="0"/>
                
                <TextBlock Text="Queue:" Style="{StaticResource MaterialDesignSubtitle2TextBlock}" Margin="0,0,0,8"/>
                <ItemsControl ItemsSource="{Binding FilesBeingProcessed}" MaxHeight="120">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Style="{StaticResource MaterialDesignCaptionTextBlock}" Foreground="{DynamicResource MaterialDesignBodyLight}" Margin="0,4"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.Template>
                        <ControlTemplate TargetType="ItemsControl">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </ControlTemplate>
                    </ItemsControl.Template>
                </ItemsControl>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
