<UserControl x:Class="PatientIntakeApp.Views.FacilitySelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <materialDesign:Card Width="450" UniformCornerRadius="8" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="24">
            <StackPanel>
                <TextBlock Text="Select Facility" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,16" />
                
                <TextBlock Text="Choose the facility ruleset to apply for this session." 
                           Style="{StaticResource MaterialDesignBody2TextBlock}" 
                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                           Margin="0,0,0,24"/>
                
                <ComboBox ItemsSource="{Binding FilteredFacilities}" 
                          SelectedItem="{Binding SelectedFacility}"
                          DisplayMemberPath="Name"
                          materialDesign:HintAssist.Hint="Select Facility"
                          IsEditable="True"
                          IsTextSearchEnabled="True"
                          TextSearch.TextPath="Name"
                          Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          Margin="0,0,0,32"/>
                
                <Button Content="CONTINUE" 
                        Command="{Binding ContinueCommand}" 
                        Style="{StaticResource NoRippleRaisedButton}"
                        HorizontalAlignment="Stretch"
                        Height="40"
                        Margin="0,0,0,16"/>

                <TextBlock Text="Logged in as: NurseUser01" 
                           Style="{StaticResource MaterialDesignCaptionTextBlock}" 
                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
